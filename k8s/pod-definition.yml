apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
  # labels are comments/tags
  labels:
     app: myapp
     type: front-end
spec:
   containers:
     - name: nginx-controller
       image: nginx
     - name: busybox
       image: busybox
